{
    "description": "Update the system built-in admin/root password via NETCONF using openconfig-aaa",
    "input_method": "stdin",
    "files": [
        "edgeops/files/task_helper.rb",
        "edgeops/lib/puppet_x/puppetlabs/netconf/client.rb",
        "edgeops/lib/puppet_x/puppetlabs/netconf/session.rb",
        "edgeops/lib/puppet_x/puppetlabs/netconf/redactor.rb"
    ],
    "remote": false,
    "parameters": {
        "password": {
            "description": "New admin password in cleartext (the device should hash and store)",
            "type": "Optional[String]"
        },
        "password_hashed": {
            "description": "Pre-hashed admin password value (device-specific hash format)",
            "type": "Optional[String]"
        },
        "target": {
            "description": "NETCONF datastore to edit",
            "type": "Enum[running, candidate]",
            "default": "candidate"
        },
        "commit": {
            "description": "Whether to commit the candidate datastore after edit (only relevant when target='candidate')",
            "type": "Boolean",
            "default": true
        },
        "redact_patterns": {
            "description": "Optional additional patterns to redact from logs/outputs",
            "type": "Optional[Array[String]"
        }
    }
}